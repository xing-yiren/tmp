name: Auto-add hardware label
on:
  issues:
    types: [opened, edited]

permissions:
  issues: write

jobs:
  label-hardware:
    uses: mindspore-courses/.github/.github/workflows/label_issue.yaml@master
    secrets: inherit